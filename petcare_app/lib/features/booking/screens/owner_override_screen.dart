import 'package:flutter/material.dart';

// TODO: Implement Sprint 5 — ADR-001 Owner Override
// Màn hình: ảnh full-screen + AI warning + countdown 5 phút + Approve/Reject
class OwnerOverrideScreen extends StatelessWidget {
  const OwnerOverrideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Owner Override — TODO')),
    );
  }
}
